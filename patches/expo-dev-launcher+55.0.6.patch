diff --git a/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift b/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift
index dca659e..d369c8f 100644
--- a/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift
+++ b/node_modules/expo-dev-launcher/ios/ReactDelegateHandler/ExpoDevLauncherReactDelegateHandler.swift
@@ -8,9 +8,9 @@ private class DevLauncherWrapperView: UIView {
   weak var devLauncherViewController: UIViewController?
   private var devLauncherConstraints: [NSLayoutConstraint] = []
 
-  #if os(iOS)
+  #if os(iOS) || os(tvOS)
   @objc
-  func orientationDidChange() {
+  func frameDidChange() {
     if let controller = devLauncherViewController {
       setDevLauncherViewControllerConstraints(controller)
     }
@@ -32,11 +32,18 @@ private class DevLauncherWrapperView: UIView {
   }
 
 #if !os(macOS)
+  override var frame: CGRect {
+    didSet {
+      UIView.animate(withDuration: 1.0, animations: {
+        self.frameDidChange()
+      })
+    }
+  }
   override func didMoveToWindow() {
     super.didMoveToWindow()
     #if os(iOS)
     if window != nil {
-      NotificationCenter.default.addObserver(self, selector: #selector(orientationDidChange), name: UIDevice.orientationDidChangeNotification, object: nil)
+      NotificationCenter.default.addObserver(self, selector: #selector(frameDidChange), name: UIDevice.orientationDidChangeNotification, object: nil)
     }
     #endif
 
diff --git a/node_modules/expo-dev-launcher/ios/SwiftUI/LocalNetworkPermissionView.swift b/node_modules/expo-dev-launcher/ios/SwiftUI/LocalNetworkPermissionView.swift
index c6eb715..4afb308 100644
--- a/node_modules/expo-dev-launcher/ios/SwiftUI/LocalNetworkPermissionView.swift
+++ b/node_modules/expo-dev-launcher/ios/SwiftUI/LocalNetworkPermissionView.swift
@@ -53,7 +53,7 @@ struct LocalNetworkPermissionView: View {
     }
     .padding(.horizontal, 24)
     .padding(.vertical, 32)
-    .background(Color(.systemBackground))
+    .background(Color.expoSystemBackground)
     .onDisappear {
       timeoutTask?.cancel()
       timeoutTask = nil
@@ -106,9 +106,11 @@ struct LocalNetworkPermissionView: View {
   }
   
   private func openSettings() {
+    #if os(iOS)
     if let url = URL(string: UIApplication.openSettingsURLString) {
       UIApplication.shared.open(url)
     }
+    #endif
   }
   
   private func continueWithoutPermission() {
@@ -147,7 +149,7 @@ struct RequestPermissionView: View {
           .multilineTextAlignment(.center)
       }
       .padding()
-      .background(Color(.secondarySystemBackground))
+      .background(Color.expoSecondarySystemBackground)
       .cornerRadius(12)
       
       Button {
diff --git a/node_modules/expo-dev-launcher/ios/SwiftUI/SettingsTabView.swift b/node_modules/expo-dev-launcher/ios/SwiftUI/SettingsTabView.swift
index 4cf47c3..a2f50d2 100644
--- a/node_modules/expo-dev-launcher/ios/SwiftUI/SettingsTabView.swift
+++ b/node_modules/expo-dev-launcher/ios/SwiftUI/SettingsTabView.swift
@@ -299,6 +299,7 @@ struct SettingsTabView: View {
 
         Divider()
 
+        #if os(iOS) || os(tvOS)
         Button {
           if let url = URL(string: UIApplication.openSettingsURLString) {
             UIApplication.shared.open(url)
@@ -312,6 +313,7 @@ struct SettingsTabView: View {
           }
         }
         .padding()
+        #endif
       }
       .background(Color.expoSecondarySystemBackground)
       .cornerRadius(12)
